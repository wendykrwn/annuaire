{% extends 'base.html.twig' %}


{% block title %}Hello AnnuaireController!{% endblock %}

{% block body %}
    <section class="crea justify-content-center ">
        <div class="card p-3 my-2" style="width: 435px">
                <div class="mb-3">
                    <label class="form-label">Nom</label>
                    <input type="text" class="form-control" placeholder="Nom du post...">
                    <label class="form-label">Description</label>
                    <input type="text" class="form-control" placeholder="Description...">
                </div>
                <div class="mb-3">
                    <button class="btn btn-primary mt-2"><i class="far fa-images"></i></button>
                    <button type="Submit" class="btn btn-primary mt-2">New post</button>  
                </div>
                
        {# {{ form_row(formPost.name, {'attr': {'placeholder': "Nom du post"}}) }}
        {{ form_row(formPost.content, {'attr': {'placeholder': "Description du post "}}) }}
        {{ form_row(formPost.image, {'attr': {'placeholder': "Url de l'image"}}) }} #}
        </div>
    </sectiion>
    <section class="posts justify-content-center ">
        {% for post in posts %}
            {{ include('_post.html.twig')}}
        {% endfor %}
    </section>
{% endblock %}